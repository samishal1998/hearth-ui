# HSidebar

> A standalone, collapsible sidebar with grouped navigation and optional custom header/footer.

[Start with the Hearth UI agent index](../../../llms.txt). This reference describes repository source; package.json version: 0.2.0. Match it to the source/release you are using.

## Identity

- Vue export: `HSidebar` from `@samishal1998/hearth-ui`.
- Custom element: `<hearth-sidebar>`.
- Individual constructor: `HearthSidebarElement` from `@samishal1998/hearth-ui/elements`.
- Category: Layouts.
- [Implementation](https://github.com/samishal1998/hearth-ui/blob/main/src/components/HSidebar.vue).

## Choose and use it correctly

- Use inside your own layout. HDashboardShell already supplies sidebar/page structure and a mobile drawer.
- sections groups navigation under headings; collapsed provides an icon rail with accessible labels. Handle update:collapsed(boolean) for externally controlled state.
- Supply icons for recognizable collapsed navigation. Header/default/footer slots permit local composition.

## Public API

brand, logo, active, label: string · items: NavItem[] · sections: {label, items: NavItem[]}[] · collapsed, collapsible: boolean

A `?` marks optional source props. A prop with a default may be omitted even when the source type is required. Internal `formDisabled` and `control-sync` plumbing is excluded below; do not use it in application code.

```ts
type Props = {
    brand?: string;
    logo?: string;
    items?: NavItem[];
    sections?: SidebarSection[];
    active?: string;
    label?: string;
    collapsed?: boolean;
    collapsible?: boolean;
};
```

### Defaults

```ts
{
    brand: "hearth",
    items: () => [],
    sections: () => [],
    label: "Workspace",
    collapsible: true,
  }
```

### Events

navigate(id), update:collapsed(boolean)

```ts
type Emits = {
    navigate: [
        id: string
    ];
    "update:collapsed": [
        collapsed: boolean
    ];
};
```

Vue-emitted custom-element events expose arguments as `event.detail`, an array. Listen directly on the element. Native DOM events such as `click` keep their normal payloads.

### Slots

header, default, footer

Vue uses `<template #slotName>`; custom elements use `<div slot="slotName">`. Custom elements do not support Vue scoped slots.

### CSS parts

base, header, navigation, footer

Use `hearth-sidebar::part(part-name)` for an exposed part, or inherited `--h-*` tokens for system-level styling. Native Vue components use the shared stylesheet and `h-*` classes.

## Supporting types

```ts
export interface NavItem {
    id: string;
    label: string;
    icon?: string;
    href?: string;
    badge?: string | number;
    disabled?: boolean;
}

export interface SidebarSection {
    label: string;
    items: NavItem[];
}
```

## Vue example

```vue
<script setup lang="ts">
import { ref } from 'vue';
import { HSidebar } from '@samishal1998/hearth-ui';
import '@samishal1998/hearth-ui/styles.css';
const active = ref('overview');
const collapsed = ref(false);
const items = [{ id: 'overview', label: 'Overview', icon: 'home' }, { id: 'apps', label: 'Applications', icon: 'apps' }];
</script>

<template>
  <HSidebar brand="homestead" :items="items" :active="active" v-model:collapsed="collapsed" @navigate="active = $event" />
</template>
```

## Web-component example

Provide a mount point (use a `form` instead of `div` when demonstrating field submission):

```html
<div id="example"></div>
```

The following is bundler-based ESM. For raw HTML, load `dist/themes.css` with a stylesheet link and import `registerElements` from `./dist/elements/index.js` instead. Set array/object properties before connection when they should become reset defaults.

```js
import { registerElements } from '@samishal1998/hearth-ui/elements';
import '@samishal1998/hearth-ui/themes.css';
registerElements();

const mount = document.querySelector('#example');
const element = document.createElement('hearth-sidebar');
Object.assign(element, {
  "brand": "homestead",
  "active": "overview",
  "items": [
    {
      "id": "overview",
      "label": "Overview",
      "icon": "home"
    },
    {
      "id": "apps",
      "label": "Applications",
      "icon": "apps"
    }
  ]
});
element.addEventListener('navigate', event => { element.active = event.detail[0]; });
mount.append(element);
```

## Related components

- [HDashboardShell](../dashboard-shell/llms.txt): A complete dashboard frame with desktop sidebar, native mobile navigation dialog, account controls, and content area.
- [HNavigationMenu](../navigation-menu/llms.txt): A navigation disclosure menu with native links, nested groups, outside-click dismissal, and Escape support.
- [HBrand](../brand/llms.txt): A text wordmark with the signature accent. Use your own name, optional logo, and tagline.

---
Generated by `npm run docs:generate` from component source, `site/catalog.ts`, and `scripts/agent-examples.mjs`. Do not edit this generated file directly.
