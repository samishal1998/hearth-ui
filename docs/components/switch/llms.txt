# HSwitch

> A keyboard-operable switch backed by a real checkbox, with form participation in custom-element mode.

[Start with the Hearth UI agent index](../../../llms.txt). This reference describes repository source; package.json version: 0.2.0. Match it to the source/release you are using.

## Identity

- Vue export: `HSwitch` from `@samishal1998/hearth-ui`.
- Custom element: `<hearth-switch>`.
- Individual constructor: `HearthSwitchElement` from `@samishal1998/hearth-ui/elements`.
- Category: Controls.
- [Implementation](https://github.com/samishal1998/hearth-ui/blob/main/src/components/HSwitch.vue).

## Choose and use it correctly

- Use for an on/off setting. Use HCheckbox for selection or agreement and HRadioGroup for one choice among alternatives.
- The underlying checkbox exposes role=switch. Checked submits the value string; unchecked submits nothing. The model/event payload is boolean.

## Public API

modelValue, checked, disabled, required: boolean · label, description, name, value: string

A `?` marks optional source props. A prop with a default may be omitted even when the source type is required. Internal `formDisabled` and `control-sync` plumbing is excluded below; do not use it in application code.

```ts
type Props = {
    modelValue?: boolean;
    checked?: boolean;
    label: string;
    description?: string;
    name?: string;
    value?: string;
    disabled?: boolean;
    required?: boolean;
};
```

### Defaults

```ts
{ modelValue: undefined, checked: false, value: "on" }
```

### Events

update:modelValue(checked), change(checked)

```ts
type Emits = {
    "update:modelValue": [
        value: boolean
    ];
    change: [
        value: boolean
    ];
};
```

Vue-emitted custom-element events expose arguments as `event.detail`, an array. Listen directly on the element. Native DOM events such as `click` keep their normal payloads.

### Slots

None

Vue uses `<template #slotName>`; custom elements use `<div slot="slotName">`. Custom elements do not support Vue scoped slots.

### CSS parts

base, label, description, control

Use `hearth-switch::part(part-name)` for an exposed part, or inherited `--h-*` tokens for system-level styling. Native Vue components use the shared stylesheet and `h-*` classes.

## Vue example

```vue
<script setup lang="ts">
import { ref } from 'vue';
import { HSwitch } from '@samishal1998/hearth-ui';
import '@samishal1998/hearth-ui/styles.css';
const visible = ref(false);
</script>

<template>
  <HSwitch v-model="visible" label="Public dashboard" description="Allow anonymous read-only visits." name="public" value="yes" />
</template>
```

## Web-component example

Provide a mount point (use a `form` instead of `div` when demonstrating field submission):

```html
<div id="example"></div>
```

The following is bundler-based ESM. For raw HTML, load `dist/themes.css` with a stylesheet link and import `registerElements` from `./dist/elements/index.js` instead. Set array/object properties before connection when they should become reset defaults.

```js
import { registerElements } from '@samishal1998/hearth-ui/elements';
import '@samishal1998/hearth-ui/themes.css';
registerElements();

const mount = document.querySelector('#example');
const element = document.createElement('hearth-switch');
Object.assign(element, {
  "label": "Public dashboard",
  "description": "Allow anonymous read-only visits.",
  "name": "public",
  "value": "yes",
  "checked": false
});
element.addEventListener('change', event => {
  const [value] = event.detail;
  // Update your application state using value.
});
mount.append(element);
```

## Related components

- [HCheckbox](../checkbox/llms.txt): A native checkbox with a mixed-state option and optional description.
- [HRadioGroup](../radio-group/llms.txt): One native radio group per component, with descriptions, disabled choices, and arrow/Home/End keyboard control.

---
Generated by `npm run docs:generate` from component source, `site/catalog.ts`, and `scripts/agent-examples.mjs`. Do not edit this generated file directly.
