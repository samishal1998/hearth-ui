# HNavigationMenu

> A navigation disclosure menu with native links, nested groups, outside-click dismissal, and Escape support.

[Start with the Hearth UI agent index](../../../llms.txt). This reference describes repository source; package.json version: 0.2.0. Match it to the source/release you are using.

## Identity

- Vue export: `HNavigationMenu` from `@samishal1998/hearth-ui`.
- Custom element: `<hearth-navigation-menu>`.
- Individual constructor: `HearthNavigationMenuElement` from `@samishal1998/hearth-ui/elements`.
- Category: Layouts.
- [Implementation](https://github.com/samishal1998/hearth-ui/blob/main/src/components/HNavigationMenu.vue).

## Choose and use it correctly

- Uses navigation links and native disclosure groups, not ARIA application-menu/menuitem roles.
- Groups use children: NavItem[]. ArrowDown on a group trigger enters its enabled links; Escape closes an open group and returns focus. Tab remains native.
- href supplies native navigation. Without href, navigate(id) is an application action; the library does not install a router.
- Popup positioning is local; avoid clipping ancestors when you need its full flyout visible.

## Public API

items: (NavItem & {children?: NavItem[]})[] · active, label: string

A `?` marks optional source props. A prop with a default may be omitted even when the source type is required. Internal `formDisabled` and `control-sync` plumbing is excluded below; do not use it in application code.

```ts
type Props = {
    items: NavigationItem[];
    active?: string;
    label?: string;
};
```

### Defaults

```ts
{ items: () => [], label: "Main navigation" }
```

### Events

navigate(id)

```ts
type Emits = {
    navigate: [
        id: string
    ];
};
```

Vue-emitted custom-element events expose arguments as `event.detail`, an array. Listen directly on the element. Native DOM events such as `click` keep their normal payloads.

### Slots

None

Vue uses `<template #slotName>`; custom elements use `<div slot="slotName">`. Custom elements do not support Vue scoped slots.

### CSS parts

base, trigger, popup, item

Use `hearth-navigation-menu::part(part-name)` for an exposed part, or inherited `--h-*` tokens for system-level styling. Native Vue components use the shared stylesheet and `h-*` classes.

## Supporting types

```ts
export interface NavigationItem extends NavItem {
    children?: NavItem[];
}

export interface NavItem {
    id: string;
    label: string;
    icon?: string;
    href?: string;
    badge?: string | number;
    disabled?: boolean;
}
```

## Vue example

```vue
<script setup lang="ts">
import { ref } from 'vue';
import { HNavigationMenu } from '@samishal1998/hearth-ui';
import '@samishal1998/hearth-ui/styles.css';
const active = ref('home');
const items = [{ id: 'home', label: 'Home', href: '/' }, { id: 'manage', label: 'Manage', children: [{ id: 'apps', label: 'Applications' }, { id: 'providers', label: 'Providers' }] }];
</script>

<template>
  <HNavigationMenu :items="items" :active="active" label="Primary navigation" @navigate="active = $event" />
</template>
```

## Web-component example

Provide a mount point (use a `form` instead of `div` when demonstrating field submission):

```html
<div id="example"></div>
```

The following is bundler-based ESM. For raw HTML, load `dist/themes.css` with a stylesheet link and import `registerElements` from `./dist/elements/index.js` instead. Set array/object properties before connection when they should become reset defaults.

```js
import { registerElements } from '@samishal1998/hearth-ui/elements';
import '@samishal1998/hearth-ui/themes.css';
registerElements();

const mount = document.querySelector('#example');
const element = document.createElement('hearth-navigation-menu');
Object.assign(element, {
  "label": "Primary navigation",
  "active": "home",
  "items": [
    {
      "id": "home",
      "label": "Home",
      "href": "/"
    },
    {
      "id": "manage",
      "label": "Manage",
      "children": [
        {
          "id": "apps",
          "label": "Applications"
        },
        {
          "id": "providers",
          "label": "Providers"
        }
      ]
    }
  ]
});
element.addEventListener('navigate', event => {
  const [id] = event.detail;
  element.active = id; // Integrate your router here.
});
mount.append(element);
```

## Related components

- [HPublicShell](../public-shell/llms.txt): A public landing-page frame: brand, a few links, an account action, and a quiet footer.
- [HSidebar](../sidebar/llms.txt): A standalone, collapsible sidebar with grouped navigation and optional custom header/footer.
- [HBreadcrumbs](../breadcrumbs/llms.txt): A wrapping breadcrumb trail with an accessible current page.

---
Generated by `npm run docs:generate` from component source, `site/catalog.ts`, and `scripts/agent-examples.mjs`. Do not edit this generated file directly.
