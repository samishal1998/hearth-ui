# HAppCard

> A launchable application card with source, status, and optional owner controls. Favorite state belongs to your app.

[Start with the Hearth UI agent index](../../../llms.txt). This reference describes repository source; package.json version: 0.2.0. Match it to the source/release you are using.

## Identity

- Vue export: `HAppCard` from `@samishal1998/hearth-ui`.
- Custom element: `<hearth-app-card>`.
- Individual constructor: `HearthAppCardElement` from `@samishal1998/hearth-ui/elements`.
- Category: Surfaces.
- [Implementation](https://github.com/samishal1998/hearth-ui/blob/main/src/components/HAppCard.vue).

## Choose and use it correctly

- favorite is consumer-controlled. Update it after favorite-change(checked); persist it in application storage if needed.
- editable exposes owner controls; it is a presentation flag, not authorization. Enforce permissions on your server.
- name/url/metadata are application data. The component does not discover services, probe health, or retrieve provenance. HTTP(S)/relative launch URLs are supported; unsafe schemes are not launched.

## Public API

name, url, description, category, image, source, target: string · status: healthy | unknown | degraded | unreachable | missing · editable, favorite: boolean

A `?` marks optional source props. A prop with a default may be omitted even when the source type is required. Internal `formDisabled` and `control-sync` plumbing is excluded below; do not use it in application code.

```ts
type Props = {
    name: string;
    url?: string;
    description?: string;
    category?: string;
    image?: string;
    status?: "healthy" | "unknown" | "degraded" | "unreachable" | "missing";
    source?: string;
    favorite?: boolean;
    editable?: boolean;
    target?: string;
};
```

### Defaults

```ts
{ status: "unknown", source: "Self-hosted", target: "_blank" }
```

### Events

favorite-change(checked), inspect(), edit()

```ts
type Emits = {
    "favorite-change": [
        favorite: boolean
    ];
    inspect: [
    ];
    edit: [
    ];
};
```

Vue-emitted custom-element events expose arguments as `event.detail`, an array. Listen directly on the element. Native DOM events such as `click` keep their normal payloads.

### Slots

default (beside category)

Vue uses `<template #slotName>`; custom elements use `<div slot="slotName">`. Custom elements do not support Vue scoped slots.

### CSS parts

base, app-icon, title, description, footer, favorite

Use `hearth-app-card::part(part-name)` for an exposed part, or inherited `--h-*` tokens for system-level styling. Native Vue components use the shared stylesheet and `h-*` classes.

## Vue example

```vue
<script setup lang="ts">
import { ref } from 'vue';
import { HAppCard } from '@samishal1998/hearth-ui';
import '@samishal1998/hearth-ui/styles.css';
const favorite = ref(false);
function edit() { /* Open your editor. */ }
function inspect() { /* Show your source information. */ }
</script>

<template>
  <HAppCard name="Photos" url="https://photos.example.com" description="Our photo library." category="Media" status="healthy" editable :favorite="favorite" @favorite-change="favorite = $event" @edit="edit" @inspect="inspect" />
</template>
```

## Web-component example

Provide a mount point (use a `form` instead of `div` when demonstrating field submission):

```html
<div id="example"></div>
```

The following is bundler-based ESM. For raw HTML, load `dist/themes.css` with a stylesheet link and import `registerElements` from `./dist/elements/index.js` instead. Set array/object properties before connection when they should become reset defaults.

```js
import { registerElements } from '@samishal1998/hearth-ui/elements';
import '@samishal1998/hearth-ui/themes.css';
registerElements();

const mount = document.querySelector('#example');
const element = document.createElement('hearth-app-card');
Object.assign(element, {
  "name": "Photos",
  "url": "https://photos.example.com",
  "description": "Our photo library.",
  "category": "Media",
  "status": "healthy",
  "editable": true,
  "favorite": false
});
element.addEventListener('favorite-change', event => { element.favorite = event.detail[0]; });
element.addEventListener('edit', () => { /* Open your editor. */ });
element.addEventListener('inspect', () => { /* Show source information. */ });
mount.append(element);
```

## Related components

- [HCard](../card/llms.txt): The common surface: restrained depth, a readable heading, and generous space for your content.
- [HBadge](../badge/llms.txt): A compact label with optional status dot. Color always accompanies text.
- [HEmptyState](../empty-state/llms.txt): An inviting first-run or no-results state, with room for a useful next action.

---
Generated by `npm run docs:generate` from component source, `site/catalog.ts`, and `scripts/agent-examples.mjs`. Do not edit this generated file directly.
