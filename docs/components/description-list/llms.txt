# HDescriptionList

> Responsive native key/value metadata with links and per-value slots.

[Start with the Hearth UI agent index](../../../llms.txt). This describes repository source for version 0.5.0. Match documentation to your installed source/release.

## Identity

- Vue export: `HDescriptionList` from `@hearth-ui/vue`.
- Custom element: `<hearth-description-list>`.
- Constructor: `HearthDescriptionListElement` from `@hearth-ui/elements`.
- Category: Surfaces.
- [Implementation](https://github.com/samishal1998/hearth-ui/blob/main/src/components/HDescriptionList.vue).

## Choose and use it correctly

- Native dl/dt/dd semantics suit resource metadata and provenance. Values are escaped primitive text by default.
- Override a value through a slot named value:<item.key>. Use HDataTable for comparable records with columns.

## Public API

items: DescriptionItem[] · columns: 1 | 2 | 3

A `?` marks optional source props. Props with defaults may be omitted. Internal `formDisabled` and `control-sync` plumbing is excluded below; do not use it in application code.

```ts
type Props = {
    items: DescriptionItem[];
    columns?: 1 | 2 | 3;
};
```

### Defaults

```ts
{
  items: () => [],
  columns: 1,
}
```

### Events

None

Vue-emitted custom-element events expose argument arrays in `event.detail`. Listen on the element. Native events such as `click` retain their normal payloads. The elements package provides framework-independent DOM event types.

### Slots

value:<item.key>

Vue uses `<template #slotName>`; custom elements use `<div slot="slotName">`. Web components do not support Vue scoped slots.

### CSS parts

base, item, label, value

Use `hearth-description-list::part(part-name)` for an exposed part or inherited `--h-*` tokens for shared styling. Native Vue components use the shared stylesheet and `h-*` classes.

## Supporting types

```ts
export interface DescriptionItem {
    key: string;
    label: string;
    value?: TableCell;
    href?: string;
}

export type TableCell = string | number | boolean | null | undefined;
```

## Vue example

```vue
<script setup lang="ts">
import { HDescriptionList } from '@hearth-ui/vue';
import '@hearth-ui/vue/styles.css';
const fields = [{key:'version',label:'Version',value:'1.0.0'},{key:'endpoint',label:'Endpoint',value:'https://example.com',href:'https://example.com'}];
</script>

<template>
  <HDescriptionList :items="fields" :columns="2" />
</template>
```

## Web-component example

Provide a mount point. For field submission, use an owning native form; the auth page already contains its own form.

```html
<div id="example"></div>
```

This is bundler-based ESM. For raw HTML, load `dist/themes.css` with a link and import `registerElements` from `./dist/elements/index.js` instead. Initial array/object properties are assigned before connection to establish reset defaults.

```js
import { registerElements } from '@hearth-ui/elements';
import '@hearth-ui/elements/themes.css';
registerElements();

const mount = document.querySelector('#example');
const element = document.createElement('hearth-description-list');
Object.assign(element, {
  "columns": 2,
  "items": [
    {
      "key": "version",
      "label": "Version",
      "value": "1.0.0"
    },
    {
      "key": "endpoint",
      "label": "Endpoint",
      "value": "https://example.com",
      "href": "https://example.com"
    }
  ]
});

mount.append(element);
```

## Related components

- [HResourceDetail](../resource-detail/llms.txt): A resource introduction with breadcrumbs, state, metadata, and tabbed details.
- [HCopyField](../copy-field/llms.txt): A read-only endpoint, identifier, or secret field with explicit clipboard and reveal actions.
- [HDataTable](../data-table/llms.txt): Semantic data tables with sorting, selection, paging, loading/empty states, and row actions.

---
Generated by `npm run docs:generate` from component source, `site/catalog.ts`, and `scripts/agent-examples.mjs`. Do not edit this generated file directly.
