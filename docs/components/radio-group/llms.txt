# HRadioGroup

> One native radio group per component, with descriptions, disabled choices, and arrow/Home/End keyboard control.

[Start with the Hearth UI agent index](../../../llms.txt). This reference describes repository source; package.json version: 0.2.0. Match it to the source/release you are using.

## Identity

- Vue export: `HRadioGroup` from `@samishal1998/hearth-ui`.
- Custom element: `<hearth-radio-group>`.
- Individual constructor: `HearthRadioGroupElement` from `@samishal1998/hearth-ui/elements`.
- Category: Controls.
- [Implementation](https://github.com/samishal1998/hearth-ui/blob/main/src/components/HRadioGroup.vue).

## Choose and use it correctly

- Use the whole group rather than inventing individual hearth-radio elements. Radios in separate shadow roots do not share native grouping.
- Each group is one string-valued form field. Give separate fields separate names.
- Arrow keys, Home, and End move between enabled choices. Options can include descriptions and disabled states.

## Public API

label, name, modelValue, value, description, error: string · options: {value, label, description?, disabled?}[] · required, disabled: boolean · orientation: horizontal | vertical

A `?` marks optional source props. A prop with a default may be omitted even when the source type is required. Internal `formDisabled` and `control-sync` plumbing is excluded below; do not use it in application code.

```ts
type Props = {
    modelValue?: string;
    value?: string;
    label: string;
    name?: string;
    options: ChoiceOption[];
    description?: string;
    error?: string;
    disabled?: boolean;
    required?: boolean;
    orientation?: "horizontal" | "vertical";
};
```

### Defaults

```ts
{ value: "", options: () => [], orientation: "vertical" }
```

### Events

update:modelValue(value), change(value)

```ts
type Emits = {
    "update:modelValue": [
        value: string
    ];
    change: [
        value: string
    ];
};
```

Vue-emitted custom-element events expose arguments as `event.detail`, an array. Listen directly on the element. Native DOM events such as `click` keep their normal payloads.

### Slots

None

Vue uses `<template #slotName>`; custom elements use `<div slot="slotName">`. Custom elements do not support Vue scoped slots.

### CSS parts

base, label, option, control

Use `hearth-radio-group::part(part-name)` for an exposed part, or inherited `--h-*` tokens for system-level styling. Native Vue components use the shared stylesheet and `h-*` classes.

## Supporting types

```ts
export interface ChoiceOption extends SelectOption {
    description?: string;
}

export interface SelectOption {
    value: string;
    label: string;
    disabled?: boolean;
}
```

## Vue example

```vue
<script setup lang="ts">
import { ref } from 'vue';
import { HRadioGroup } from '@samishal1998/hearth-ui';
import '@samishal1998/hearth-ui/styles.css';
const access = ref('private');
const options = [{ value: 'private', label: 'Private' }, { value: 'public', label: 'Public' }];
</script>

<template>
  <HRadioGroup v-model="access" label="Page access" name="access" :options="options" required />
</template>
```

## Web-component example

Provide a mount point (use a `form` instead of `div` when demonstrating field submission):

```html
<div id="example"></div>
```

The following is bundler-based ESM. For raw HTML, load `dist/themes.css` with a stylesheet link and import `registerElements` from `./dist/elements/index.js` instead. Set array/object properties before connection when they should become reset defaults.

```js
import { registerElements } from '@samishal1998/hearth-ui/elements';
import '@samishal1998/hearth-ui/themes.css';
registerElements();

const mount = document.querySelector('#example');
const element = document.createElement('hearth-radio-group');
Object.assign(element, {
  "label": "Page access",
  "name": "access",
  "value": "private",
  "required": true,
  "options": [
    {
      "value": "private",
      "label": "Private"
    },
    {
      "value": "public",
      "label": "Public"
    }
  ]
});
element.addEventListener('change', event => {
  const [value] = event.detail;
  // Update your application state using value.
});
mount.append(element);
```

## Related components

- [HSelect](../select/llms.txt): A native select with labeled options, not a recreated browser picker.
- [HCheckbox](../checkbox/llms.txt): A native checkbox with a mixed-state option and optional description.
- [HSwitch](../switch/llms.txt): A keyboard-operable switch backed by a real checkbox, with form participation in custom-element mode.

---
Generated by `npm run docs:generate` from component source, `site/catalog.ts`, and `scripts/agent-examples.mjs`. Do not edit this generated file directly.
