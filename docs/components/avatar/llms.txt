# HAvatar

> An image avatar with initials fallback and an optional decorative mode.

[Start with the Hearth UI agent index](../../../llms.txt). This reference describes repository source; package.json version: 0.2.0. Match it to the source/release you are using.

## Identity

- Vue export: `HAvatar` from `@samishal1998/hearth-ui`.
- Custom element: `<hearth-avatar>`.
- Individual constructor: `HearthAvatarElement` from `@samishal1998/hearth-ui/elements`.
- Category: Foundation.
- [Implementation](https://github.com/samishal1998/hearth-ui/blob/main/src/components/HAvatar.vue).

## Choose and use it correctly

- name supplies the accessible name and initials fallback. src is optional; failed images fall back to initials.
- Use decorative when a nearby visible name already labels the person. This is not an interactive profile menu.

## Public API

name, src: string · size: number = 40 · shape: circle | rounded · tone: Tone · decorative: boolean

A `?` marks optional source props. A prop with a default may be omitted even when the source type is required. Internal `formDisabled` and `control-sync` plumbing is excluded below; do not use it in application code.

```ts
type Props = {
    name: string;
    src?: string;
    size?: number;
    shape?: "circle" | "rounded";
    tone?: Tone;
    decorative?: boolean;
};
```

### Defaults

```ts
{ size: 40, shape: "circle", tone: "accent" }
```

### Events

None

Vue-emitted custom-element events expose arguments as `event.detail`, an array. Listen directly on the element. Native DOM events such as `click` keep their normal payloads.

### Slots

None

Vue uses `<template #slotName>`; custom elements use `<div slot="slotName">`. Custom elements do not support Vue scoped slots.

### CSS parts

base, image, fallback

Use `hearth-avatar::part(part-name)` for an exposed part, or inherited `--h-*` tokens for system-level styling. Native Vue components use the shared stylesheet and `h-*` classes.

## Supporting types

```ts
export type Tone = "neutral" | "accent" | "success" | "warning" | "danger" | "info";
```

## Vue example

```vue
<script setup lang="ts">
import { HAvatar } from '@samishal1998/hearth-ui';
import '@samishal1998/hearth-ui/styles.css';

</script>

<template>
  <HAvatar name="Sami Mishal" :size="40" tone="accent" />
</template>
```

## Web-component example

Provide a mount point (use a `form` instead of `div` when demonstrating field submission):

```html
<div id="example"></div>
```

The following is bundler-based ESM. For raw HTML, load `dist/themes.css` with a stylesheet link and import `registerElements` from `./dist/elements/index.js` instead. Set array/object properties before connection when they should become reset defaults.

```js
import { registerElements } from '@samishal1998/hearth-ui/elements';
import '@samishal1998/hearth-ui/themes.css';
registerElements();

const mount = document.querySelector('#example');
const element = document.createElement('hearth-avatar');
Object.assign(element, {
  "name": "Sami Mishal",
  "size": 40,
  "tone": "accent"
});
mount.append(element);
```

## Related components

- [HBrand](../brand/llms.txt): A text wordmark with the signature accent. Use your own name, optional logo, and tagline.
- [HNavigationMenu](../navigation-menu/llms.txt): A navigation disclosure menu with native links, nested groups, outside-click dismissal, and Escape support.
- [HSkeleton](../skeleton/llms.txt): Text, rectangle, and circular loading placeholders with reduced-motion support.

---
Generated by `npm run docs:generate` from component source, `site/catalog.ts`, and `scripts/agent-examples.mjs`. Do not edit this generated file directly.
